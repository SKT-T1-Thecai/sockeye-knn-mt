# to do a simple forward in a pretrained translation model
def once_forward(model,sample):
    raise NotImplementedError

class my_str(str):
    def __init__(self) -> None:
        super().__init__()

print(isinstance(45,int))
print(isinstance(45,str))
print(isinstance("asdf",my_str))